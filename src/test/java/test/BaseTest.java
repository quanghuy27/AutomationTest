package test;

import commons.GlobalVariable;

public class BaseTest {

//    protected void navigateByURL(String url) {
//        GlobalVariable.getDriver().get(url);
//        GlobalVariable.getDriver().manage().window().maximize();
//    }
}
